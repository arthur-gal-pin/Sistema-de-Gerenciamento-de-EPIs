BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TB_INSTRUMENTO] ALTER COLUMN [FuncaoInstrumento] NVARCHAR(500) NULL;

-- AlterTable
ALTER TABLE [dbo].[TB_PROTOCOLO] ADD [Descricao] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
